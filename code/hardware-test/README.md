# Config

1. Install [nodejs](https://nodejs.org/en/download)
2. `apt install sox`
3. `npm install`
4. `mv .env.example .env`
5. `node test-harware.js`


** to detect the devices you can also use the detect-hw.sh script. be ready to unplug and plug the button. **
